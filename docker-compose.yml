services:
  watcher:
    build: .
    container_name: tpo-watcher
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - watcher_data:/app/data
    environment:
      # Persistent paths in mounted volume
      - DB_PATH=/app/data/watcher.sqlite
      - STATE_FILE=/app/data/playwright_state.json

volumes:
  watcher_data:
